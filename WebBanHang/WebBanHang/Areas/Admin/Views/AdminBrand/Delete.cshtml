@model WebBanHang.Context.Brand

@{
    ViewBag.Title = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Xóa nhãn hàng</h2>
<p>@ViewBag.error</p>
<h3>Bạn muốn xóa nhãn hàng @Model.Name ?</h3>
<div>
    <hr />
  <table class="table table-bordered">
      <tr>
          <th>
              Name
          </th>
          <th>
              Hình đại diện
          </th>
      </tr>
      <tr>
          <td>
              @Model.Name
          </td>
          <td>
              <img src="@Url.Content("~/Content/images/items/" + Model.Avatar)" width="100" />
          </td>
      </tr>
  </table>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Xóa" class="btn btn-default" onclick="alert('Xóa nhãn hàng này!')"/> |
            @Html.ActionLink("Trở về", "Index")
        </div>
    }
</div>
